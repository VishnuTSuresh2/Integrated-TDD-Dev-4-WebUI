import App from "./App";
import {expect} from "chai";

let $=require('jquery');

export default function(){
    
    describe("App",()=>{
        it("can navigate to absolute url of path in app",()=>{
            expect(0).to.equal(1);
        })
    })
}